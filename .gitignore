__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/
debian/*.debhelper
debian/*.debhelper.log
debian/*.substvars
debian/cuems-power-bridge/
debian/files
debian/.debhelper/
debian/debhelper-build-stamp

# uv is only a test runner here (the project builds with Poetry); its lock
# file is a local artifact, not project state.
uv.lock

# Wheel built from the ../cuems-utils checkout at build time so pip can resolve
# cuemsutils >=0.1.0rc16, which is not on PyPI. Stripped from the .deb again.
debian/.cuemsutils-wheel/
